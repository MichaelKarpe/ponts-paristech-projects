#pragma once

/// Algorithms constants

// Coeff for the computation of JW distance
const double coeff_winkler = 0.1;

// Threshold for the hierarchic clustering algorithm
const double HC_threshold = 0.8;

// Nb occurences d'un groupe de mot à partir duquel on le garde (à voir)
const int nb_group = 5;
